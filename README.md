# auditram-text-highlighter
This project is a Python-based solution. It allows a user to upload a file, search for a specific text string, and generate a new output file where all matched text is visually highlighted using red, unfilled bounding boxes, without modifying the original document.
